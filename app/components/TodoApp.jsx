var React = require("react");

var TodoApp = React.createClass({
    render: function () {
        return (
            <div>
                to do app jsx
            </div>
        )
    }
});

module.exports = TodoApp;
